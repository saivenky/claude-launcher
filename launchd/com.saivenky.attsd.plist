<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  attsd LaunchAgent template.

  Before loading:
    1. Replace __SERVER_PY__ with the absolute path to server.py
       e.g. /Users/you/projects/attsd/server.py
    2. Replace __HOME__ with your home directory — launchd does not expand
       `~`, so both log paths must be absolute.
    3. (Optional) Adjust EnvironmentVariables.PATH if your `python3`
       lives somewhere unusual.
    4. Copy to ~/Library/LaunchAgents/
    5. launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.saivenky.attsd.plist
    6. launchctl kickstart -k gui/$UID/com.saivenky.attsd
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.saivenky.attsd</string>

    <key>ProgramArguments</key>
    <array>
        <string>/usr/bin/env</string>
        <string>python3</string>
        <string>__SERVER_PY__</string>
    </array>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>

        <!-- Config lives here, not in the repo. Add whichever you need; the
             README's environment table lists them all. The installed copy is
             the only record of these, so back it up before you touch it.

        <key>ATTSD_DEFAULT_DIR</key>
        <string>/Users/you/somewhere</string>
        <key>ATTSD_TOKEN</key>
        <string>a-shared-secret-enabling-Respond</string>
        -->
    </dict>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <true/>

    <key>StandardOutPath</key>
    <string>__HOME__/Library/Logs/attsd.log</string>

    <key>StandardErrorPath</key>
    <string>__HOME__/Library/Logs/attsd.log</string>

    <key>ProcessType</key>
    <string>Background</string>
</dict>
</plist>
